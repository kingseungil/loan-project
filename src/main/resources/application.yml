spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-docker:3306/loan?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 1234

  jpa:
    database: mysql
    hibernate:
      ddl-auto: create
    show-sql: true

  data:
    redis:
      host: redis-docker
      port: 6379

  security:
    user:
      name: test
      password: test

  encrypt:
    algorithm: AES
    key: 1234567890123456

jwt:
  secret:
    key: dGVzdAo=
